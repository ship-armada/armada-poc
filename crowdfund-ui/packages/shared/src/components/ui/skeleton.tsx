// ABOUTME: shadcn/ui "skeleton" primitive — dark-themed via shared theme tokens.
// ABOUTME: Generated by shadcn CLI; edit in place, never wrap.
import { cn } from "../../lib/utils.js"

function Skeleton({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="skeleton"
      className={cn("animate-pulse rounded-md bg-accent", className)}
      {...props}
    />
  )
}

export { Skeleton }
